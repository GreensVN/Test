# ============================================================================
# TẦNG NC — GIỌNG AI PHI THƯƠNG MẠI NHƯNG ĐƯỢC MỞ MÃ NGUỒN
# ============================================================================
#
# ĐỌC TRƯỚC KHI CÀI:
#   Mọi mô hình liệt kê ở đây đều CẤM DÙNG THƯƠNG MẠI.
#   Bạn vẫn được mở mã nguồn dự án, với điều kiện KHÔNG đóng gói file
#   trọng số vào repo.
#
#   Xem bảng giấy phép:  python giong_nc.py giay-phep
#   Xác nhận trước khi dùng:  python giong_nc.py dong-y mms-vie
#
# CÁCH CÀI (đừng cài cả file một lần — cài từng nhóm):
#   pip install -r requirements-giong-nc.txt
# hoặc chạy: cai_dat_giong_ai.bat rồi chọn tầng NC
#
# ============================================================================
# NHÓM 1 — BẮT BUỘC cho mms-tts-vie (mô hình NC nhẹ nhất, ~145MB)
# ============================================================================
# Giấy phép weights: CC BY-NC 4.0 — phi thương mại, KHÔNG ShareAlike
# Đây là mô hình DUY NHẤT mà giong_nc.py chạy trực tiếp được.

transformers>=4.33.0
numpy>=1.24.0

# torch: KHÔNG pin phiên bản vì phụ thuộc CPU/GPU và phiên bản Python.
# Bản CPU cho Windows (nhẹ hơn nhiều, ~200MB thay vì ~2.5GB):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# Bản CUDA 12.1 nếu bạn có card NVIDIA:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0

# ============================================================================
# NHÓM 2 — TUỲ CHỌN: VietTTS (chất lượng cao hơn, có clone giọng)
# ============================================================================
# Code Apache-2.0 / Weights CC BY-NC 4.0 — không ShareAlike.
# Lựa chọn cân bằng tốt nhất của tầng NC.
# Cài riêng vì kéo theo nhiều phụ thuộc nặng:
#   pip install viet-tts
# Repo: https://github.com/dangvansam/viet-tts

# viet-tts

# ============================================================================
# NHÓM 3 — TUỲ CHỌN: viXTTS (clone giọng 6 giây, chất lượng cao nhất)
# ============================================================================
# Weights CPML — phi thương mại, SIẾT CẢ ÂM THANH ĐẦU RA.
# Dùng bản fork còn bảo trì của idiap, KHÔNG dùng coqui-ai/TTS đã bỏ hoang:
#   pip install coqui-tts
#   git clone https://github.com/thinhlpg/vixtts-demo
# Weights: https://huggingface.co/capleaf/viXTTS

# coqui-tts

# ============================================================================
# NHÓM 4 — TUỲ CHỌN: F5-TTS Vietnamese (1000 giờ tiếng Việt)
# ============================================================================
# Weights CC BY-NC-SA 4.0 — CÓ ShareAlike.
# Nếu bạn fine-tune tiếp, mô hình mới BẮT BUỘC cũng phải CC BY-NC-SA 4.0.
#   pip install f5-tts
# Weights: https://huggingface.co/hynt/F5-TTS-Vietnamese-ViVoice

# f5-tts

# ============================================================================
# KHÔNG BAO GIỜ CÀI
# ============================================================================
# - vi_VN-25hours_single-low : giấy phép ghi "Unknown".
#   Không có giấy phép = không được cấp quyền nào = không mở mã nguồn được.
#   Trường hợp này TỆ HƠN phi thương mại.
#
# - Các bản fine-tune trên HuggingFace tự gắn nhãn "mit" trong khi checkpoint
#   gốc là CC BY-NC. Người upload không có quyền nới lỏng giấy phép của người
#   khác. Luôn truy ngược lên mô hình gốc.
#
# ============================================================================
# THÊM VÀO .gitignore CỦA BẠN
# ============================================================================
#   giong_nc_model/
#   giong_ai/
#   *.onnx
#   *.pt
#   *.pth
#   *.safetensors
#
# Rồi chạy: python giong_nc.py notice
# để sinh GIAY_PHEP_MODEL.md — commit file đó kèm repo.
