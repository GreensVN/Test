# ============================================================
# Trợ lý ảo tiếng Việt v7.2 - Requirements
# Chạy ngay không cần cài gì (dùng LiteModel thuần Python)
# Cài thêm để nâng cao độ chính xác / giọng nói
# ============================================================

# --- Core khuyến nghị (TF-IDF chính xác hơn Lite) ---
# Tự chọn phiên bản có wheel dựng sẵn theo Python version
scikit-learn>=1.3.0,<1.4.0 ; python_version < "3.9"
joblib>=1.3.0,<1.5.0       ; python_version < "3.9"

scikit-learn>=1.5.0 ; python_version >= "3.9"
joblib>=1.4.0       ; python_version >= "3.9"

# --- Fuzzy matching chất lượng cao (tuỳ chọn, fallback difflib) ---
rapidfuzz>=3.0.0,<3.9.0 ; python_version < "3.9"
rapidfuzz>=3.9.0        ; python_version >= "3.9"

# --- TTS (giọng nói ra) - tuỳ chọn ---
# pyttsx3: offline, khuyến nghị
# gTTS: online, giọng tự nhiên nhất, cần mạng
# playsound: phát file mp3 block (tránh chồng tiếng)
pyttsx3>=2.90
gTTS>=2.5.0
# playsound chỉ cần trên Windows, Linux/macOS dùng afplay/mpg123 có sẵn
# playsound==1.2.2 ; sys_platform == "win32"

# --- STT (nhận diện giọng nói) - tuỳ chọn ---
# Cách 1 nhẹ (cần internet mỗi lần nói):
# SpeechRecognition>=3.10.0
# sounddevice>=0.4.6
# numpy>=1.24.0

# Cách 2 offline chính xác nhất cho tiếng Việt (PhoWhisper ~1GB):
# transformers>=4.37.0
# torch>=2.0.0
# sounddevice>=0.4.6

# --- Điều khiển âm lượng chính xác trên Windows (tuỳ chọn) ---
# pycaw>=20240210 ; sys_platform == "win32"
# comtypes>=1.2.0 ; sys_platform == "win32"

# --- DataFrame / Colab ---
# pandas>=2.2.0

# --- Test ---
# pytest>=7.0.0
# pytest-cov>=4.0.0
# ruff>=0.1.0
